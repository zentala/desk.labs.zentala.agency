---
import { getCollection } from 'astro:content';
import '../../styles/global.css';
import '../../styles/fonts.css';

const posts = (await getCollection('blog')).sort(
  (a, b) => new Date(b.data.date).getTime() - new Date(a.data.date).getTime()
);
---
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <!-- Dev preview: not for indexing. Remove before launch — see TASKS.md "Pre-launch blockers". -->
  <meta name="robots" content="noindex, nofollow">
  <title>Blog — zntlDesk</title>
  <meta name="description" content="Articles about sitting habits, standing desk ergonomics, and the science of movement at work.">
  <link rel="icon" type="image/svg+xml" href="/favicon.svg">
  <link rel="alternate" type="application/rss+xml" title="zntlDesk Blog" href="/blog/rss.xml">
  <script defer data-domain="lp.desk.labs.zentala.agency" src="https://plausible.io/js/script.js"></script>
</head>
<body>
  <div class="mx-auto max-w-6xl px-4 py-16">
    <a href="/" class="text-sm text-ink-muted hover:text-ink transition-colors">
      &larr; Back to Home
    </a>

    <header class="mt-8 mb-16">
      <h1 class="text-3xl font-bold sm:text-4xl">Blog</h1>
      <p class="mt-4 text-lg text-ink-muted">
        Insights on sitting habits, desk ergonomics, and building healthier work routines.
      </p>
    </header>

    <div class="grid gap-8 sm:grid-cols-2">
      {posts.map((post) => {
        const formattedDate = new Date(post.data.date).toLocaleDateString('en-US', {
          year: 'numeric',
          month: 'short',
          day: 'numeric',
        });
        return (
          <a
            href={`/blog/${post.id}`}
            class="group block rounded-2xl border border-line bg-surface p-6 transition-colors hover:border-line"
          >
            <time class="text-xs text-ink-muted">{formattedDate}</time>
            <h2 class="mt-2 text-xl font-bold text-ink group-hover:text-brand-text transition-colors">
              {post.data.title}
            </h2>
            <p class="mt-3 text-sm text-ink-muted leading-relaxed">
              {post.data.description}
            </p>
            {post.data.tags.length > 0 && (
              <div class="mt-4 flex flex-wrap gap-2">
                {post.data.tags.map((tag: string) => (
                  <span class="rounded-full border border-line bg-surface-2 px-2 py-0.5 text-xs text-ink-muted">
                    {tag}
                  </span>
                ))}
              </div>
            )}
          </a>
        );
      })}
    </div>
  </div>
</body>
</html>
