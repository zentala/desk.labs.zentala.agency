---
import '../styles/global.css';
import '../styles/fonts.css';

interface Props {
  title: string;
  description?: string;
  date: string;
  author: string;
  tags: string[];
}

const { title, description, date, author, tags } = Astro.props;
const formattedDate = new Date(date).toLocaleDateString('en-US', {
  year: 'numeric',
  month: 'long',
  day: 'numeric',
});
---
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <!-- Dev preview: not for indexing. Remove before launch — see TASKS.md "Pre-launch blockers". -->
  <meta name="robots" content="noindex, nofollow">
  <title>{title} — zntlDesk Blog</title>
  <meta name="description" content={description || title}>
  <link rel="icon" type="image/svg+xml" href="/favicon.svg">
  <script defer data-domain="lp.desk.labs.zentala.agency" src="https://plausible.io/js/script.js"></script>
</head>
<body>
  <article class="mx-auto max-w-[720px] px-4 py-16">
    <a href="/blog" class="text-sm text-ink-muted hover:text-ink transition-colors">
      &larr; Back to Blog
    </a>

    <header class="mt-8 mb-12">
      <h1 class="text-3xl font-bold sm:text-4xl leading-tight">
        {title}
      </h1>
      <div class="mt-4 flex flex-wrap items-center gap-4 text-sm text-ink-muted">
        <time datetime={date}>{formattedDate}</time>
        <span>by {author}</span>
      </div>
      {tags.length > 0 && (
        <div class="mt-4 flex flex-wrap gap-2">
          {tags.map((tag: string) => (
            <span class="rounded-full border border-line bg-surface-2 px-3 py-1 text-xs text-ink-muted">
              {tag}
            </span>
          ))}
        </div>
      )}
    </header>

    <div class="prose prose-site prose-lg max-w-none
      [&_h2]:text-2xl [&_h2]:font-bold [&_h2]:mt-12 [&_h2]:mb-4
      [&_h3]:text-xl [&_h3]:font-semibold [&_h3]:mt-8 [&_h3]:mb-3
      [&_p]:text-ink-muted [&_p]:leading-relaxed [&_p]:mb-4
      [&_ul]:list-disc [&_ul]:pl-6 [&_ul]:mb-4 [&_ul]:text-ink-muted
      [&_ol]:list-decimal [&_ol]:pl-6 [&_ol]:mb-4 [&_ol]:text-ink-muted
      [&_li]:mb-2
      [&_strong]:text-ink
      [&_a]:text-brand-text [&_a]:underline [&_a:hover]:text-brand-text
      [&_blockquote]:border-l-2 [&_blockquote]:border-brand-strong/40 [&_blockquote]:pl-4 [&_blockquote]:italic [&_blockquote]:text-ink-muted
      [&_hr]:border-line [&_hr]:my-8">
      <slot />
    </div>

    <footer class="mt-16 border-t border-line pt-8">
      <a href="/blog" class="text-sm text-ink-muted hover:text-ink transition-colors">
        &larr; Back to Blog
      </a>
    </footer>
  </article>
</body>
</html>
